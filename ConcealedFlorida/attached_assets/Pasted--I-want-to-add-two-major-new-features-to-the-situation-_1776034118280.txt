"I want to add two major new features to the situation room map page at /we-are-watching/map:
FEATURE 1 — EMERGENCY ALERT SCROLLING BANNER
Add a red scrolling ticker banner fixed at the very top of the map page above the map itself. It should continuously scroll horizontally like a news ticker showing active emergency alerts. Pull alerts from two sources server-side every 5 minutes:
Source 1 — NOAA Active Alerts at https://api.weather.gov/alerts/active?status=actual&severity=Extreme,Severe,Moderate — filter to show only Extreme and Severe alerts. Parse the event name, area description, and severity from each alert.
Source 2 — FEMA IPAWS Archived Alerts at https://www.fema.gov/api/open/v2/ipawsArchivedAlerts?orderby=sent+desc&
top=20
 — parse the event type and area from each record.
Combine both sources into a single deduplicated list sorted by most recent first. Display each alert in the ticker as: ALERT TYPE — AFFECTED AREA — TIME ISSUED. Color code the ticker background: red for Extreme, orange for Severe, yellow for Moderate. If there are no active alerts display a green banner that says ALL CLEAR — No Active National Emergencies. The ticker should auto-scroll and loop continuously. Add a small NOAA logo and FEMA logo on the left side of the banner crediting the sources.
FEATURE 2 — NEW MAP LAYERS
Add the following new toggleable layers to the existing map layer control panel:
Layer — Earthquakes: Fetch from https://earthquake.usgs.gov/earthquakes/feed/v1.0/summary/significant_week.geojson — no API key needed. Display as purple circle markers sized by magnitude. Popup shows magnitude, location, depth, and time. Label in toggle panel as Earthquakes (Past 7 Days). Default OFF. Credit USGS in the footer.
Layer — Active Wildfires: Fetch from the NASA FIRMS API — use the public CSV endpoint at https://firms.modaps.eosdis.nasa.gov/data/active_fire/modis-c6.1/csv/MODIS_C6_1_Global_24h.csv — no key required for this endpoint. Parse latitude, longitude, and brightness. Display as orange-red flame markers. Cluster nearby fires together at low zoom levels. Popup shows detection time and brightness temperature. Label in toggle panel as Active Wildfires. Default OFF. Credit NASA FIRMS in the footer.
Layer — Hurricane Advisories: Fetch the National Hurricane Center RSS feed at https://www.nhc.noaa.gov/index-at.xml — parse active storm names, categories, and track data. Display active hurricane tracks as red path lines on the map if any exist. If no active hurricanes show nothing on this layer. Label in toggle panel as Hurricane Tracks. Default ON during June through November, OFF otherwise based on current month. Credit NHC in the footer.
PERFORMANCE AND DISPLAY
All new API calls server-side in Express. Cache earthquakes for 1 hour, wildfires for 2 hours, hurricane feed for 30 minutes, alert banner for 5 minutes. The alert banner must never block the map controls or layer toggle panel. Add all new data sources to the credits footer section at the bottom of the page: USGS Earthquake Hazards Program, NASA FIRMS Fire Information, NOAA National Hurricane Center. Make sure the full site still builds and runs after all changes."**